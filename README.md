
# Hangman

```shell
# Install Composer dependencies
composer install

# Run tests
./vendor/bin/phpunit tests/

# Validate against PSR2
./vendor/bin/phpcs --standard=PSR2 -np bin/ src/ tests/

# Play the game
php bin/hangman.php
```
